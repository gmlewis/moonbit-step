///|
/// Sweep::new generates a new Shape by sweeping a 2D profile along a 3D path.
/// Note that the resulting Shape has no end caps; both ends are open.
pub fn Sweep::new(path : SweepPath, profile : SweepProfile) -> Shape {
  // TODO
  {}
}
