///|
/// Shared imports for this example package.
///
/// MoonBit resolves `using` across files in the same package directory, so we
/// keep these in one place to avoid repeating them everywhere.

///|
using @cad {type SceneNode, type Vec2, type Vec3}
